<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found</title>
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="min-h-screen bg-gradient-to-br from-emerald-500 via-green-500 to-teal-500 flex items-center justify-center p-4 font-['Space_Grotesk']">
    <div class="w-full max-w-md">
        <!-- Error Card -->
        <div class="bg-white rounded-2xl shadow-2xl overflow-hidden">
            <!-- Card Header -->
            <div class="bg-gradient-to-r from-emerald-600 to-green-600 px-8 py-6">
                <h1 class="text-6xl font-bold text-white text-center">404</h1>
            </div>

            <!-- Card Content -->
            <div class="p-8">
                <div class="text-center mb-6">
                    <svg class="w-16 h-16 mx-auto text-emerald-600 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <h2 class="text-3xl font-bold text-slate-900 mb-2">Page Not Found</h2>
                    <p class="text-slate-600 text-sm">The page you're looking for doesn't exist or has been moved.</p>
                </div>

                <div class="space-y-3">
                    <a
                        href="/"
                        class="w-full block text-center px-4 py-3 bg-gradient-to-r from-emerald-600 to-green-600 text-white font-semibold rounded-lg shadow-lg hover:shadow-xl hover:from-emerald-700 hover:to-green-700 transform hover:scale-105 transition duration-200 active:scale-95"
                    >
                        Go to Home
                    </a>
                    {{#if user}}
                        <a
                            href="/dashboard"
                            class="w-full block text-center px-4 py-3 bg-slate-200 text-slate-900 font-semibold rounded-lg hover:bg-slate-300 transition duration-200"
                        >
                            Go to Dashboard
                        </a>
                    {{/if}}
                </div>
            </div>

            <!-- Card Footer -->
            <div class="border-t border-slate-200 bg-slate-50 px-8 py-4">
                <p class="text-center text-xs text-slate-500">
                    Error Code: 404
                </p>
            </div>
        </div>

        <!-- Footer Text -->
        <div class="text-center mt-8 text-emerald-100 text-sm">
            Real-time Queue Management System
        </div>
    </div>
</body>
</html>
